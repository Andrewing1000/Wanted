name: mascotas_flutter
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  #ar_flutter_plugin: ^0.6.1
  flutter_map: ^4.0.0
  latlong2: ^0.8.1
  url_launcher: ^6.1.10
  image_picker: ^0.8.5+3
  google_fonts: ^4.0.4
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/paw_logo.png
    - assets/dummy.jpg
